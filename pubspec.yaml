# pubspec.yaml
name: mvc
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+5

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated SDK constraint for latest packages

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.3
  provider: ^6.1.2     # Add this
  uuid: ^4.3.3
  image_picker: ^1.1.2
  firebase_core: ^2.28.0 # ADD THIS
  device_info_plus: ^10.1.2
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.16.1
  firebase_storage: ^11.7.2
  firebase_database: ^10.5.7
  google_sign_in: ^6.2.1
  url_launcher: ^6.3.1
  tdesign_flutter: ^0.2.2
  pedometer: ^4.1.1
  fl_chart: ^0.69.0
  flutter_svg: ^2.2.0
  intl: ^0.20.2
  profanity_filter: ^2.0.0
  confetti: ^0.7.0
  workmanager: ^0.8.0
  connectivity_plus: ^4.0.2  # For background step tracking
  pdf: ^3.11.0  # For PDF generation
  path_provider: ^2.1.3  # For file system access
  share_plus: ^9.0.0  # For sharing files
  flutter_secure_storage: ^9.2.2  # For secure configuration storage
  crypto: ^3.0.3  # For encryption and hashing
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Keeping your lints
  mockito: ^5.0.0 # Added for testing
  build_runner: ^2.4.9 # For code generation
  flutter_launcher_icons: ^0.13.1


flutter:
  uses-material-design: true
  assets: # Add assets for the health symbol if you plan to use an image
    - assets/health_logo.png
    - assets/google_logo.png # Example path, create an 'assets' folder and put an image there.
    - assets/svg/glass.svg
    - assets/svg/water.svg
    - assets/svg/bubble.svg
    # Achievement icons
    - assets/icons/water_drop.svg
    - assets/icons/footsteps.svg
    - assets/icons/sleep.svg
    - assets/icons/food.svg
    - assets/icons/meditation.svg
    - assets/icons/level.svg
    - assets/icons/trophy.svg
    - assets/icons/social.svg
    - assets/icons/app.svg
    - assets/icons/explore.svg
    - assets/icons/time.svg
    - assets/icons/seasonal.svg
    - assets/icons/challenge.svg

flutter_icons:
  android: true
  image_path: "assets/icons/app.svg"